sudo: required

notifications:
  email:
    recipients:
      - fake@email.com
    on_success: never
    on_failure: always

language: csharp

mono: none

dotnet: 2.1

install:
  - dotnet restore

script:
  - dotnet build
  - dotnet test
